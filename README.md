# SmartFile Flow

![Python](https://img.shields.io/badge/Python-3.10%2B-blue?logo=python)
![Word2Vec](https://img.shields.io/badge/ML-Word2Vec-orange?logo=tensorflow)
![Status](https://img.shields.io/badge/Status-In%20Development-yellow)

Умный сортировщик файлов с искусственным интеллектом. Автоматическая организация файлов по смыслу с использованием Word2Vec и ML. Автоматически анализирует содержимое, определяет категорию и раскладывает файлы по нужным папкам, используя Word2Vec и гибкие правила.

---

## 🤖 Интеллектуальная классификация

Семантический анализ — понимает смысл текста, а не просто ключевые слова.

Гибридная система — сочетает ML и правила для большей точности.

Предобученная модель — word2vec-ruscorpora-300 (184 973+ слов).

Автоматическая морфология — корректно работает с разными формами слов.

---

## 🚀 Быстрый старт

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
python main.py
```

---

## 📂 Поддерживаемые форматы

**Готово:**

* **TXT** — автоопределение кодировки
* **PDF** — через pdfplumber / PyPDF2
* **DOCX/DOC** — через python-docx
* **JSON** — извлечение текстовых и числовых значений из вложенных структур
* **XML** — безопасный парсер, поддержка текста узлов и атрибутов
* **Изображения (OCR)** — PNG / JPG / JPEG / TIFF / BMP / WEBP (через Tesseract + pytesseract)

**В разработке:**

* Электронные таблицы (Excel / CSV)

---

## 🛠️ TODO

* Интеграция с облачными хранилищами
* Разработка GUI-интерфейса
* Плагинная архитектура для расширения функционала
* Адаптивный GUI (desktop scaling + подготовка под планшеты)
* Темная тема интерфейса
* Локализация интерфейса (многоязычная поддержка)

---

## 📌 Требования

* Python **3.10+**
* ~**500 MB RAM** (с подключённой предобученной моделью Word2Vec ruscorpora — TODO: добавить ссылку)
* Скачайте предобученные модели (архив `models/`, веса Word2Vec, category vectors) по одной из зеркальных ссылок: [Google Drive](https://drive.google.com/drive/folders/1v7RPGwrqUGWRnRGiTzeD7jWU0aFnrYzJ?usp=drive_link) / [MEGA](https://mega.nz/file/zAU2RLyL#C6nHtqqsxf0aby25W0sl9ZeSteLATqOBLhlslbBSPOg). Разместите содержимое в папке `models/` в корне проекта.

---

## 🖼️ OCR и установка Tesseract (для изображений)

Для обработки изображений используется связка **Tesseract OCR** + **pytesseract**:

- В `requirements.txt` уже есть зависимости `pytesseract` и `Pillow`.
- Необходимо **дополнительно установить сам Tesseract как системную программу**.

### Установка Tesseract на Windows

1. Скачайте инсталлер Tesseract для Windows с официальной сборки UB Mannheim  
   (см. раздел *“Tesseract installer for Windows”* на странице проекта:  
   [`https://github.com/UB-Mannheim/tesseract/wiki`](https://github.com/UB-Mannheim/tesseract/wiki)).
2. Установите Tesseract **в рекомендованную папку по умолчанию** (обычно что-то вроде  
   `C:\Program Files\Tesseract-OCR\`).
3. (Опционально) Добавьте путь к `tesseract.exe` в переменную окружения `PATH` **или** пропишите его
   явно в `config.yaml`:

   ```yaml
   settings:
     enable_ocr: true
     ocr_backend: "tesseract"
     ocr_lang: "rus+eng"
     ocr_tesseract_cmd: "C:\\Program Files\\Tesseract-OCR\\tesseract.exe"
     ocr_preprocess: "advanced"   # "none" | "simple" | "advanced"
   ```

После этого изображения с расширениями из `image_extensions` будут автоматически прогоняться через OCR
и участвовать в той же ML/правильной классификации, что и остальные форматы.

### Режимы препроцессинга изображений (`ocr_preprocess`)

- **"none"**: без препроцессинга — в OCR летит исходное изображение как есть.  
- **"simple"**: grayscale → автоконтраст → простая бинаризация + лёгкое сглаживание шума.  
- **"advanced"** (рекомендуется): grayscale → автоконтраст → адаптивная бинаризация (Otsu‑подобная) → медианный фильтр → масштабирование мелких картинок для лучшего распознавания мелкого текста.

---

## ⭐ Почему SmartFile Flow?

Интеллектуальный подбор алгоритмов, понятный интерфейс, удобство автоматизации и гибкость настроек делают SmartFile Flow идеальным инструментом для работы с большими файловыми коллекциями.
